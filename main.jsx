import React from "react";
import ReactDOM from "react-dom/client";
import UserNotRegisteredError from "./User.jsx";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <UserNotRegisteredError />
  </React.StrictMode>
);
